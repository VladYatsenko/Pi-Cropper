<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="52dp"
    android:clickable="true"
    android:focusable="true"
    android:minWidth="52dp">

    <View
        android:id="@+id/ratio_dot"
        android:layout_width="4dp"
        android:layout_height="4dp"
        android:layout_marginHorizontal="3dp"
        app:layout_constraintBottom_toBottomOf="@+id/ratio_title"
        app:layout_constraintEnd_toStartOf="@+id/ratio_title"
        app:layout_constraintTop_toTopOf="@+id/ratio_title"
        android:background="@drawable/circle_selected" />

    <TextView
        android:id="@+id/ratio_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="2dp"
        android:includeFontPadding="false"
        app:layout_constraintVertical_bias="0.62"
        android:text="@string/crop_style_dynamic"
        android:textColor="@color/cerulean"
        android:textSize="13sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed" />

</androidx.constraintlayout.widget.ConstraintLayout>